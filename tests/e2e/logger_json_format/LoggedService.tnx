import tinox.core.logger;

@Log
class LoggedService
{
    fn new() -> LoggedService
    {
        return LoggedService {};
    }

    fn doWork(svc: LoggedService) -> Nothing
    {
        Logger::info(svc.log, "service doing work");
    }
}
