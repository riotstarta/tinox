// expect: 42

class Main
{
    fnc apply(f: fnc(Int64) -> Int64, x: Int64) -> Int64
    {
        return f(x);
    }

    fnc main() -> Int32
    {
        let double: fnc(Int64) -> Int64 = x => x * 2;
        println(apply(double, 21));
        return 0;
    }
}
