// Bug 17: test harness read Bool return values as i64 — failing tests became PASS
// mode: test
// expect-exit: 1
// expect-contains: 1 failed
class bug17_test_harness_bool {
    @Test("must fail")
    fn t() -> Bool {
        var xs: List<Int64> = [];
        xs.push(1);
        return xs.len() == 999;
    }
}
