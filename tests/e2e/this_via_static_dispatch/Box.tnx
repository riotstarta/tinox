import IntBox;
import Acc;

class Box<T> {
    var value: T;
    fn wrap(v: T) -> Box<T> { return Box { value: v }; }
    fn get() -> T { return this.value; }
    fn setAndGet(v: T) -> T { this.value = v; return this.value; }
}
