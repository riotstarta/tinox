# Change Log

## 0.1.0

- Initial release: filter the active JSON/YAML editor via the local `jgrep` CLI
- Command Palette and editor context menu: **jgrep: Filter current JSON/YAML**
- Setting `jgrep-tinox.executablePath` (default `jgrep`)
