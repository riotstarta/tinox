[package]
name = "http3_server"
version = "1.0.0"
description = "tinox.core.http3_server, split out of the bundled stdlib as a standalone tinox-central package."

[[dependencies]]
group = "tinox.core"
artifactId = "http_server"
version = "1.0.0"
