module tinox.core.debug;

namespace tinox.core.debug
{
    class Debug
    {
        fn assert(condition: Bool, message: String) -> Unit
        {
            if condition == false
            {
                Debug::panic(message);
            }
        }

        fn panic(message: String) -> Unit
        {
            throw "Assertion failed: " + message;
        }

        fn printStackTrace() -> Unit
        {
            printStackTrace();
        }

        fn typeName() -> String
        {
            return "unknown";
        }

        fn memoryUsage() -> Int64
        {
            return memoryUsage();
        }

        fn gcCollect() -> Unit
        {
            gcCollect();
        }
    }
}