module tinox.core.option;

namespace tinox.core.option
{
    class Option
    {
        fnc none() -> Option
        {
            return Option { hasValue: false, value: 0 };
        }

        fnc some(value: Int64) -> Option
        {
            return Option { hasValue: true, value: value };
        }

        fnc isSome() -> Bool
        {
            return this.hasValue;
        }

        fnc isNone() -> Bool
        {
            return this.hasValue == false;
        }

        fnc unwrap() -> Int64
        {
            if this.hasValue == false
            {
                throw "Option is None";
            }
            return this.value;
        }

        fnc unwrapOr(defaultValue: Int64) -> Int64
        {
            if this.hasValue == false
            {
                return defaultValue;
            }
            return this.value;
        }

        fnc map(transform: fnc(Int64) -> Int64) -> Option
        {
            if this.hasValue == false
            {
                return Option::none();
            }
            return Option::some(transform(this.value));
        }
    }
}