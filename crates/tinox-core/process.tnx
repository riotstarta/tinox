module tinox.core.process;

namespace tinox.core.process
{
    class Process
    {
        fn exit(code: Int64) -> Unit
        {
            processExit(code);
        }

        fn sleep(ms: Int64) -> Unit
        {
            sleep(ms);
        }

        fn pid() -> Int64
        {
            return processId();
        }

        fn args() -> List<String>
        {
            return processArgs();
        }

        fn env(name: String) -> String
        {
            return envGet(name);
        }

        fn setEnv(name: String, value: String) -> Unit
        {
            envSet(name, value);
        }
    }
}