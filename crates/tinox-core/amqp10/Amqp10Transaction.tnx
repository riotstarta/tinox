module tinox.core.amqp10;

import tinox.core.socket;
import tinox.core.base64;

namespace tinox.core.amqp10
{
    /*
     * Ergebnis von Amqp10Link::declare() (Issue 79). ok == false bei
     * jedem Fehlschlag (Broker antwortet nicht mit `declared`, o. ae.) —
     * txnId ist dann leer. Bei Erfolg wird txnId an publishTransactional()/
     * ackTransactional()/discharge() weitergereicht, analog zu 0-9-1/1.0s
     * sonstigem errorMessage-Konventionen in diesem Modul.
     */
    class Amqp10Transaction
    {
        var txnId: List<Int64>;
        var ok: Bool;
        var errorMessage: String;
    }
}
