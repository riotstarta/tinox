module tinox.core.env;

namespace tinox.core.env
{
    class Env
    {
        fn getVar(name: String) -> String
        {
            return envGet(name);
        }

        fn setVar(name: String, value: String) -> Unit
        {
            envSet(name, value);
        }

        fn removeVar(name: String) -> Unit
        {
            envRemove(name);
        }

        fn currentDir() -> String
        {
            return envCurrentDir();
        }

        fn setCurrentDir(path: String) -> Unit
        {
            envSetCurrentDir(path);
        }
    }
}