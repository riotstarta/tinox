module tinox.core.result;

namespace tinox.core.result
{
    class Result
    {
        fnc ok(value: Int64) -> Result
        {
            return Result { isOk: true, value: value, error: "" };
        }

        fnc err(error: String) -> Result
        {
            return Result { isOk: false, value: 0, error: error };
        }

        fnc isOk() -> Bool
        {
            return this.isOk;
        }

        fnc isErr() -> Bool
        {
            return this.isOk == false;
        }

        fnc unwrap() -> Int64
        {
            if this.isOk == false
            {
                throw this.error;
            }
            return this.value;
        }

        fnc unwrapOr(defaultValue: Int64) -> Int64
        {
            if this.isOk == false
            {
                return defaultValue;
            }
            return this.value;
        }

        fnc map(transform: fnc(Int64) -> Int64) -> Result
        {
            if this.isOk == false
            {
                return Result::err(this.error);
            }
            return Result::ok(transform(this.value));
        }
    }
}