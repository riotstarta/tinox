import math_lib;

class Main
{
    fnc main() -> Int32
    {
        let x = math_lib::add(3, 4);
        println(x);
        let y = math_lib::mul(3, 4);
        println(y);
        return 0;
    }
}
