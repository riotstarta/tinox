import interface_extends.IShape;
import interface_extends.IDrawable;
import interface_extends.Circle;

fn main() -> Int64
{
    let c = Circle {};
    let d: IDrawable = c;
    d.draw();
    println(d.area());
    return 0;
}
