# Build artifacts
/target
**/*.rlib
**/*.pdb

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Tinox state
.tinox_progress
.tinox_notes
*.tinox_state

# Tinox package manager: project-local dependency cache (populated by
# `tinox install` for explicit-url deps -- coordinate-resolved deps go to
# the global ~/.tinox/repository/ cache instead, never checked in either)
.tinox/
*.tar.gz
tinox.lock

# Claude Code
claude
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Temporary
*.tmp
*.bak
.tinox_tmp_*
*.ll
*.o
*.s
a.out
test_*
test
test2
test_manual
test_verify
test_O0
test_O1
runtime.o
build.rs
hello_out
test_out
work.sh
*.err
simple_out
benchmarks/*.class
benchmarks/*.bc
benchmarks/bench_fib
benchmarks/bench_fib_new
benchmarks/bench_fib_opt
benchmarks/bench_primes
benchmarks/bench_primes_new
benchmarks/bench_primes_opt
benchmarks/bench_sum
benchmarks/bench_sum_new
benchmarks/bench_sum_opt
.tinox_dev_*
massif.out.*

# Fuzzing (fuzz/, see fuzz/README.md) — built binaries and the
# fuzzer-grown working corpus/crash artifacts; fuzz/*/seeds/ is checked in
fuzz/*/*_fuzzer
fuzz/*/corpus/
fuzz/*/crashes/
fuzz/*/artifacts/
fuzz/*/crash-*
fuzz/*/leak-*
fuzz/*/timeout-*
fuzz/*/oom-*
fuzz/*/slow-unit-*
