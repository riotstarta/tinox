import tinox.core.amqp10;
import tinox.core.socket;

class Amqp10Driver
{
    fnc tinoxAmqp10ReadFrame(conn: Int64) -> Int64
    {
        let f = Amqp10::readFrame(conn);
        return f.frameType;
    }
}
