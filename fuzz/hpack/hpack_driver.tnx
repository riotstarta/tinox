import tinox.core.hpack;

fn tinoxHpackDecode(bytes: List<Int64>) -> Int64
{
    let dynTable: HpackDynTable = HpackDynTable::new(4096);
    let headers: List<HpackHeader> = Hpack::decode(bytes, dynTable);
    return headers.len();
}
