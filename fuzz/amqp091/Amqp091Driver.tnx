import tinox.core.amqp091;
import tinox.core.socket;

class Amqp091Driver
{
    fnc tinoxAmqp091ReadFrame(conn: Int64) -> Int64
    {
        let f = Amqp091::readFrame(conn);
        return f.frameType;
    }
}
